defaults:
  - training: defaults
  - model_dict : defaults


save_dir : ${hydra:runtime.cwd}/data
local_data_npz: ${hydra:runtime.cwd}/data/5-cassette
training_data: ${hydra:runtime.cwd}/data/cassette-th-data.pck

training_args :
  model_type: "transformer_encoder"


